info:
  name: registracija
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5199/api/auth/register
  body:
    type: json
    data: |-
      {
        "firstName": "John",
        "lastName": "Doe",
        "email": "jajadoe@example.com",
        "password": "Password1",
        "phone": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
