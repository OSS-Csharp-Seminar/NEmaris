info:
  name: registracija
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5199/api/auth/register
  body:
    type: json
    data: |-
      {
        "firstName": "Jajo",
        "lastName": "Jajic",
        "email": "jajkan@example.com",
        "password": "jaja",
        "phone": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
