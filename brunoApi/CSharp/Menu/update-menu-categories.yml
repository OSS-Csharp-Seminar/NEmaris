info:
  name: update-menu-categories
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:5199/api/menu-categories/2
  body:
    type: json
    data: |-
      {
        "name": "Breakfast",
        "description": "Updated breakfast menu",
        "displayOrder": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
