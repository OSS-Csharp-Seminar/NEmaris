info:
  name: update-menu-categories
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:5000/api/menu-categories
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
