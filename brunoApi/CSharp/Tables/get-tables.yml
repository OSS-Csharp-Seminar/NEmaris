info:
  name: get-tables
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:5199/api/tables
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
