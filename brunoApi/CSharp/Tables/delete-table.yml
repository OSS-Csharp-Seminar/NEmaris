info:
  name: delete-table
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:5199/api/tables/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
