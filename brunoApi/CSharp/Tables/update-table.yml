info:
  name: update-table
  type: http
  seq: 2

http:
  method: PUT
  url: http://localhost:5199/api/tables/1
  body:
    type: json
    data: |-
      {
        "tableNumber": "A-01",
        "capacity": 6,
        "zone": "Terrace",
        "status": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
