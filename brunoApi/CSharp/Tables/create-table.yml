info:
  name: create-table
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5199/api/tables
  body:
    type: json
    data: |-
      {
        "tableNumber": "A-01",
        "capacity": 4,
        "zone": "Main hall",
        "status": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
