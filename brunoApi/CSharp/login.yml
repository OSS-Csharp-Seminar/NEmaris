info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5199/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "jajkan@example.com",
        "password": "jaja2121A"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
